Braden Steller & Jacob O'Quinn

To run the program:
    Navigate to the directory of the folder in a command line
    Run the 'make' command
    The makefile will take care of compiling all of the sourcecode files together
    Run the program with the command format:
        ./a.out {<filename>.txt} [-l] [-a] [-v]
        Where {} is required and [] is optional